# Codex command conventions

- After every completed code or configuration change, commit and push all relevant changes.
- When the user sends `/commit`, commit and push all relevant changes.
- When the user sends `/commit-and-discard`, commit and push all relevant changes, then discard all other uncommitted changes.
- Before discarding changes, verify which changes are relevant and preserve the committed work.
